# Systematically traverses all cells of a Rubik's Cube and populates the cells
# with an integer from 1 to 24 corresponding to the order in which they were
# visited.

[
    setd 1 + gtp
    F setd 1 + gtp
    F setd 1 + gtp
    F setd 1 + gtp
    F x setd 1 + gtp
    F setd 1 + gtp
    F setd 1 + gtp
    F setd 1 + gtp
    F y
]